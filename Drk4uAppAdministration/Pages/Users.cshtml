@page
@model Drk4uAppAdministration.Pages.UsersModel

@{
    ViewData["Title"] = "Users";
}

<div class="container">
    <div class="row mb-4">
        <div class="col">
            <h1 class="display-4">@ViewData["Title"]</h1>
            <hr />
        </div>
    </div>
    <div class="row">
        <div class="col">
            <table class="table table-bordered">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Name</th>
                        <th>Created At</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.Users.Count == 0) {
                        <tr>
                            <td colspan="3"><i>No users available.</i></td>
                        </tr>
                    } else {
                        @foreach (var user in Model.Users) {
                            <tr>
                                <td>@user.Id</td>
                                <td>@user.Name</td>
                                <td>@user.CreatedAt.ToString()</td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>